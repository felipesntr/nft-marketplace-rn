import * as React from 'react';

import {useState} from 'react';
import {View, SafeArea, FlatList, Text, Button} from 'react-native';

import {COLORS, NFTData} from '../../constants';

const Home = () => {
  return <SafeArea style={{flex: 1}}></SafeArea>;
};

export default Home;
